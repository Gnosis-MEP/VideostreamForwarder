from setuptools import setup

setup(
    name='videostreamforwarder',
    version='0.1',
    description='Forwards to the subscriber query a video stream (HLS) based on the query id.',
    author='Felipe Arruda Pontes',
    author_email='felipe.arruda.pontes@insight-centre.org',
    packages=['videostreamforwarder'],
    zip_safe=False
)
